name: E2E Test

on:
  workflow_call:
    inputs:
      runner:
        description: "JSON array of runs-on labels, e.g. [\"ubuntu-latest\"] or [\"self-hosted\",\"linux-arm64-npu-static-8\"]"
        type: string
        default: '["ubuntu-latest"]'
      mark:
        description: "pytest marker expression; empty string = all (no -m filter)"
        type: string
        default: ""
      run-as-root:
        type: boolean
        default: false
      ref:
        description: "Git ref to checkout; empty = event default"
        type: string
        default: ""
      artifact-name:
        description: "Artifact name suffix; empty = auto by arch (arm64/x86)"
        type: string
        default: ""
      xdist:
        description: "Enable pytest-xdist parallel execution (-n auto)"
        type: boolean
        default: false
      sweep-scope:
        description: "sweep scope: npu strips non-NPU cleanup (dmsetup/losetup/iptables/...); empty/full = full cleanup"
        type: string
        default: ""
      timeout-minutes:
        description: "Job timeout in minutes (E2E full NPU needs >60: 280 cases + shared-host hccn_tool contention)"
        type: number
        default: 60

permissions:
  contents: read

jobs:
  e2e:
    name: E2E (${{ inputs.mark || 'ALL' }})
    runs-on: ${{ fromJSON(inputs.runner) }}
    timeout-minutes: ${{ inputs.timeout-minutes }}
    steps:
      - name: Checkout
        id: co1
        uses: actions/checkout@v7
        with:
          fetch-depth: 1
          ref: ${{ inputs.ref }}
          # issue_comment 调用方(e2e_command /e2e)checkout fork PR head 触发 pwn 护栏,opt-in;
          # 对 pull_request/nightly 调用者是 no-op(非受信上下文不触发护栏)。
          allow-unsafe-pr-checkout: true
        # NPU 自托管 runner 到 github.com:443 不稳定，仅 self-hosted 时启用 2 次重试（共 3 次尝试）
        continue-on-error: ${{ contains(inputs.runner, 'self-hosted') }}
      - name: Checkout (retry 1/2)
        if: ${{ contains(inputs.runner, 'self-hosted') && steps.co1.outcome == 'failure' }}
        id: co2
        uses: actions/checkout@v7
        with:
          fetch-depth: 1
          ref: ${{ inputs.ref }}
          allow-unsafe-pr-checkout: true
        continue-on-error: true
      - name: Checkout (retry 2/2)
        if: ${{ contains(inputs.runner, 'self-hosted') && steps.co2.outcome == 'failure' }}
        uses: actions/checkout@v7
        with:
          fetch-depth: 1
          ref: ${{ inputs.ref }}
          allow-unsafe-pr-checkout: true

      - name: Install build deps (only if missing)
        run: |
          if ! command -v cmake >/dev/null 2>&1 || ! command -v gcc >/dev/null 2>&1; then
            bash scripts/install_deps.sh --build
          fi

      - name: Install runtime deps (only if missing)
        continue-on-error: true
        run: |
          MISS=""
          for t in ip ethtool iptables tc perl; do
            command -v "$t" >/dev/null 2>&1 || MISS=1
          done
          if [ -n "$MISS" ]; then
            bash scripts/install_deps.sh --runtime
          else
            echo "runtime deps present, skip install"
          fi

      - name: Ensure ccache (self-hosted only)
        if: ${{ contains(inputs.runner, 'self-hosted') }}
        continue-on-error: true
        run: |
          if command -v ccache >/dev/null 2>&1; then
            echo "ccache present"
          else
            sudo apt-get update -qq || true
            sudo apt-get install -y -qq ccache || true
          fi

      - name: Configure
        run: |
          CCACHE_EXTRA=""
          if command -v ccache >/dev/null 2>&1; then
            CCACHE_EXTRA="-DCMAKE_C_COMPILER_LAUNCHER=ccache"
          fi
          # shellcheck disable=SC2086
          cmake -B build -DCMAKE_BUILD_TYPE=Release $CCACHE_EXTRA

      - name: Build
        run: cmake --build build -j"$(nproc)"

      - name: Ensure python3 + curl
        run: |
          command -v python3 >/dev/null 2>&1 || sudo apt-get install -y -qq python3 || bash scripts/install_deps.sh --runtime
          command -v curl >/dev/null 2>&1 || sudo apt-get install -y -qq curl || bash scripts/install_deps.sh --runtime

      - name: Install pytest deps (only if missing)
        run: |
          if sudo -n python3 -c "import pytest, openpyxl, xdist" 2>/dev/null; then
            echo "pytest deps present, skip install"
          else
            sudo python3 -m pip install -q --break-system-packages -r tests/e2e/requirements.txt
          fi

      - name: Run E2E (pytest)
        env:
          MARK_EXPR: ${{ inputs.mark }}
          AS_ROOT: ${{ inputs.run-as-root }}
          XDIST: ${{ inputs.xdist }}
          DCAT_E2E_SWEEP_SCOPE: ${{ inputs.sweep-scope }}
        run: |
          # 清除 self-hosted runner 残留 __pycache__（sudo find 确保删 root 拥有的 stale .pyc）
          sudo find tests/e2e -name '__pycache__' -type d -exec rm -rf {} + 2>/dev/null || true
          MARK=()
          if [ -n "$MARK_EXPR" ]; then
            MARK=(-m "$MARK_EXPR")
          fi
          XDIST_FLAG=()
          if [ "$XDIST" = "true" ]; then
            XDIST_FLAG=(-n 2 --dist loadgroup)
          fi
          set +e
          if [ "$AS_ROOT" = "true" ]; then
            # shellcheck disable=SC2024
            # sudo -n: 自托管 runner 未配 passwordless sudo 时立即失败, 而非卡死拖到 60min 超时
            sudo -n -E env PYTHONDONTWRITEBYTECODE=1 GITHUB_STEP_SUMMARY="$GITHUB_STEP_SUMMARY" python3 -m pytest tests/e2e/ "${XDIST_FLAG[@]}" --junitxml=tests/e2e/e2e_junit.xml "${MARK[@]}" > tests/e2e/e2e_run.log 2>&1
            # 必须在 chown 前捕获 pytest 退出码！此前 TEST_RC 取的是
            # `sudo chown ... || true`（恒 0），把 NPU 真实测试失败吞掉 → 门禁假绿。
            TEST_RC=$?
            sudo chown "$(id -un):$(id -gn)" tests/e2e/report*.md tests/e2e/results_*.csv tests/e2e/e2e_junit.xml 2>/dev/null || true
          else
            DCAT_AUTO_SUDO=1 PYTHONDONTWRITEBYTECODE=1 GITHUB_STEP_SUMMARY="$GITHUB_STEP_SUMMARY" python3 -m pytest tests/e2e/ "${XDIST_FLAG[@]}" --junitxml=tests/e2e/e2e_junit.xml "${MARK[@]}" > tests/e2e/e2e_run.log 2>&1
            TEST_RC=$?
          fi
          set -e
          cat tests/e2e/e2e_run.log
          # GHA 只解析 step 实时 stdout 中的 ::error:: 注解；pytest 输出进了 log，
          # 此处 re-echo 才算数，保证失败用例在 PR Checks 界面直接可见（红点）
          grep -hE '^::error::' tests/e2e/e2e_run.log 2>/dev/null || true
          exit "$TEST_RC"

      - name: E2E summary
        if: always()
        run: |
          if [ -f tests/e2e/report.md ]; then
            cat tests/e2e/report.md >> "$GITHUB_STEP_SUMMARY"
          elif compgen -G "tests/e2e/report_gw*.md" > /dev/null 2>&1; then
            echo "## E2E 摘要 (pytest-xdist 并行)" >> "$GITHUB_STEP_SUMMARY"
            for f in tests/e2e/report_gw*.md; do
              [ -e "$f" ] || continue
              echo "" >> "$GITHUB_STEP_SUMMARY"
              cat "$f" >> "$GITHUB_STEP_SUMMARY"
            done
          else
            {
              echo "## E2E 摘要"
              echo "_(report 未生成: pytest 可能未运行或异常退出)_"
              echo ""
              echo "**日志见 artifact e2e-run.log**"
            } >> "$GITHUB_STEP_SUMMARY"
          fi

      - name: Print failure summary (on failure)
        if: failure()
        run: |
          echo "===== E2E 失败诊断摘要 ====="
          for f in tests/e2e/failures_*.log; do
            [ -e "$f" ] || continue
            echo "--- $f (失败用例完整 stdout/stderr/verify 输出) ---"
            cat "$f" 2>/dev/null || true
          done
          echo "--- report.md (汇总+失败用例表) ---"
          cat tests/e2e/report.md 2>/dev/null || true
          echo "===== 完整产物已作为 artifact 上传 ====="

      - name: Upload E2E results
        if: always()
        uses: actions/upload-artifact@v7
        with:
          # arch 标签避免 matrix(x86/arm 两腿)同名冲突；
          # 仅上传本次运行相关产物：report.md(含逐用例测试目的) + results_*.csv(含 expected_behavior 列) + 日志
          # 不含 cases.csv(全量用例库 355 行，非本次运行特有，避免 artifact 冗余)
          name: e2e-${{ inputs.artifact-name || ((contains(inputs.runner, 'arm') || contains(inputs.runner, 'ARM')) && 'arm64' || 'x86') }}
          path: |
            tests/e2e/e2e_run.log
            tests/e2e/failures_*.log
            tests/e2e/results_*.csv
            tests/e2e/report.md
            tests/e2e/e2e_junit.xml
          if-no-files-found: ignore
          retention-days: 7
